name: 🚀 Deploy to Hostinger via SSH

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v3

      - name: 🧱 Install dependencies
        run: npm install

      - name: 🏗️ Build static site
        run: npm run build

      - name: 📤 Upload via SCP
        uses: appleboy/scp-action@master
        with:
          host: 145.79.14.76 # ganti sesuai Hostinger kamu
          username: u240274568 # ganti sesuai Hostinger kamu
          key: ${{ secrets.HOSTINGER_SSH_KEY }}
          port: 65002
          source: "out/*"
          target: "public_html"
