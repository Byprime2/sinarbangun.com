name: 🚀 Deploy to Hostinger via SSH

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v3

      - name: 🧱 Install dependencies
        run: npm install

      - name: 🏗️ Build static site
        run: npm run build

      - name: 🧪 Debug output folder
        run: ls -la out

      - name: 📤 Upload via SCP
        uses: appleboy/scp-action@master
        with:
          host: 145.79.14.76
          username: u240274568
          key: ${{ secrets.HOSTINGER_SSH_KEY }}
          port: 65002
          source: "out/**"
          target: "./public_html"
          strip_components: 1

      - name: 🔍 Check files on Hostinger (optional)
        uses: appleboy/ssh-action@master
        with:
          host: 145.79.14.76
          username: u240274568
          key: ${{ secrets.HOSTINGER_SSH_KEY }}
          port: 65002
          script: |
            ls -la public_html
